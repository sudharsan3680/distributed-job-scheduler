\documentclass[11pt]{article}

\usepackage[margin=0.9in]{geometry}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{amsmath}
\usepackage{xurl}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, shapes.geometric}
\usepackage{hyperref}

\hypersetup{colorlinks=true, urlcolor=black, linkcolor=black, citecolor=black}
\urlstyle{same}

\definecolor{ink}{RGB}{17,24,39}
\definecolor{rule}{RGB}{30,41,59}
\definecolor{panel}{RGB}{244,244,245}

\title{\textbf{Distributed Job Scheduler}\\[2pt]\Large System Design \& Implementation Report}
\author{Submission for Distributed Job Scheduling Platform Evaluation \\
\texttt{sudhansudharsan3680@gmail.com}}
\date{2026}

\begin{document}
\sloppy
\maketitle
\noindent\rule{\textwidth}{1.2pt}\\[-2pt]
\noindent\textbf{Repository:} \url{https://github.com/sudharsan3680/distributed-job-scheduler}
\qquad\textbf{Stack:} FastAPI · SQLAlchemy 2.0 · PostgreSQL · Alembic · React/TS · WebSocket
\\[-6pt]\noindent\rule{\textwidth}{1.2pt}

\section*{1. Executive Summary}
A production-inspired, multi-tenant distributed job scheduling platform that reliably executes
asynchronous background jobs across a fleet of competing workers. PostgreSQL acts as both the
system of record and the work queue; the API is stateless and horizontally scalable; workers are
independent processes that claim work over HTTP. The platform covers the full requirement set:
authentication and project/queue management, all job types (immediate, delayed, scheduled, cron,
batch), atomic claiming, the complete job lifecycle with retries and a dead-letter queue,
configurable backoff, execution logs, and a live monitoring dashboard.

\section*{2. System Architecture}
\begin{center}
\begin{tikzpicture}[
    font=\small,
    box/.style={rectangle, draw=rule, rounded corners=3pt, minimum width=2.9cm, minimum height=1.0cm, align=center, fill=panel},
    db/.style={cylinder, draw=rule, minimum width=2.3cm, minimum height=1.5cm, align=center, fill=panel, shape border rotate=90, text centered},
    arr/.style={-{Latex[length=2.2mm]}, thick, black!70}
  ]
  \node[box] (dash) {React Dashboard};
  \node[box, below left=1.7cm and 0.3cm of dash] (api) {FastAPI REST};
  \node[box, below right=1.7cm and 0.3cm of dash] (ws) {WebSocket Fanout};
  \node[box, below left=1.5cm and 0.1cm of api] (workers) {Worker Fleet};
  \node[box, below=1.5cm of api] (sched) {Scheduler Loop};
  \node[db, below right=1.5cm and 0.1cm of api] (pg) {PostgreSQL};

  \draw[arr] (dash) -- (api);
  \draw[arr] (dash) -- (ws);
  \draw[arr, <->] (workers) -- (api) node[midway, left, font=\tiny, align=center] {claim / heartbeat\\start / result};
  \draw[arr] (sched) -- (pg);
  \draw[arr, <->] (api) -- (pg) node[midway, right, font=\tiny, align=center] {FOR UPDATE\\SKIP LOCKED};
  \draw[arr] (api) -- (ws);
  \draw[arr] (ws) -- (dash);
\end{tikzpicture}
\end{center}
The database is the single source of truth and the only component that must be made highly
available. Workers never touch the database directly, so there is no dual-source-of-truth
consistency problem. Postgres-as-coordinator (instead of a Redis/RabbitMQ broker) trades a lower
throughput ceiling for one piece of infrastructure to run and reason about; queue sharding is the
documented next step if that ceiling is reached.

\section*{3. Requirements Coverage}
\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{Requirement} & \textbf{Status / Where} \\
\midrule
Auth + project/queue management & JWT users + orgs; projects scoped by API key; RBAC (owner/admin/member/viewer) enforced on all mutating routes. \\
Queue config & priority, max\_concurrency, retry policy, pause/resume, per-queue rate limit, statistics. \\
All job types & immediate, delayed, scheduled, recurring (cron via \texttt{croniter}), batch (up to 1000). \\
Worker service & polls, atomic claim, concurrent execution (\texttt{Semaphore}), heartbeats, graceful drain. \\
Lifecycle & QUEUED $\to$ SCHEDULED $\to$ CLAIMED $\to$ RUNNING $\to$ COMPLETED, with FAILED + DEAD\_LETTER. \\
Retries & fixed / linear / exponential backoff with cap + full jitter. \\
Observability & job\_executions (per attempt), job\_logs, worker heartbeats, dashboard metrics. \\
Dashboard & queue health, worker status, job explorer, execution logs, retry/replay, throughput chart. \\
\bottomrule
\end{tabularx}

\section*{4. Database Design}
3NF relational schema, surrogate BIGINT PKs; natural keys carry their own UNIQUE constraints.
Scheduling definitions (\texttt{scheduled\_jobs}) are separate from job instances (\texttt{jobs});
\texttt{dead\_letter\_queue} is its own table (narrow DLQ scan + frozen payload snapshot).
\begin{center}
\begin{tikzpicture}[
    font=\small,
    ent/.style={rectangle, draw=rule, minimum width=2.2cm, minimum height=0.85cm, align=center, fill=panel},
    arr/.style={-, thick, black!65}
  ]
  \node[ent] (org) {Organization};
  \node[ent, right=1.15cm of org] (proj) {Project};
  \node[ent, right=1.15cm of proj] (queue) {Queue};
  \node[ent, right=1.15cm of queue] (job) {Job};
  \node[ent, below=0.95cm of job] (exec) {JobExecution};
  \node[ent, below left=0.95cm and 0.25cm of job] (log) {JobLog};
  \node[ent, below right=0.95cm and 0.25cm of job] (dlq) {DeadLetter};
  \node[ent, above right=0.95cm and 0.25cm of job] (sched) {ScheduledJob};
  \node[ent, above left=0.95cm and 0.25cm of job] (batch) {Batch};
  \node[ent, below=0.95cm of queue] (rp) {RetryPolicy};
  \node[ent, below=0.95cm of proj] (worker) {Worker};

  \draw[arr] (org) -- (proj);
  \draw[arr] (proj) -- (queue);
  \draw[arr] (queue) -- (job);
  \draw[arr] (job) -- (exec);
  \draw[arr] (job) -- (log);
  \draw[arr] (job) -- (dlq);
  \draw[arr] (queue) -- (sched);
  \draw[arr] (queue) -- (batch);
  \draw[arr] (queue) -- (rp);
  \draw[arr] (proj) -- (worker);
\end{tikzpicture}
\end{center}
Key indexes: \texttt{ix\_jobs\_claim\_scan(queue\_id, status, run\_at)} (the atomic claim scan),
\texttt{ix\_scheduled\_jobs\_due(is\_active, next\_run\_at)}, \texttt{ix\_heartbeats\_worker\_time},
\texttt{ix\_job\_logs\_job\_created}. Cascades: org $\to$ project $\to$ queue $\to$ job $\to$
executions/logs (CASCADE); user/worker $\to$ attribution (SET NULL) so audit history survives deletions.

\section*{5. Job Lifecycle}
\begin{center}
\begin{tikzpicture}[
    font=\small,
    st/.style={circle, draw=rule, minimum size=1.5cm, align=center, fill=panel},
    arr/.style={-{Latex[length=2.2mm]}, thick, black!70},
    darr/.style={-{Latex[length=2.2mm]}, thick, black!50, dashed}
  ]
  \node[st] (q) at (0,0) {Queued};
  \node[st] (s) at (2.7,0) {Scheduled};
  \node[st] (c) at (0,-2.5) {Claimed};
  \node[st] (r) at (2.7,-2.5) {Running};
  \node[st] (comp) at (5.4,0) {Completed};
  \node[st] (f) at (2.7,-5.0) {Failed};
  \node[st] (dlq) at (5.4,-5.0) {Dead\\Letter};
  \node[st] (can) at (0,-5.0) {Cancelled};

  \draw[arr] (q) -- (c);
  \draw[arr] (s) -- (q);
  \draw[arr] (c) -- (r);
  \draw[arr] (r) -- (comp);
  \draw[arr] (r) -- (f);
  \draw[arr] (f) -- (q);
  \draw[arr] (f) -- (dlq);
  \draw[darr] (q) -- (can);
  \draw[darr] (c) -- (can);
  \draw[darr] (r) -- (can);
\end{tikzpicture}
\end{center}
A retriable failure lands in FAILED (visible in the dashboard) until its backoff elapses, then the
claim path promotes it back to QUEUED. Once attempts are exhausted the job moves to DEAD\_LETTER with
a frozen payload snapshot.

\section*{6. Reliability \& Concurrency}
\begin{itemize}
  \item \textbf{Atomic claiming:} \texttt{SELECT \ldots FOR UPDATE SKIP LOCKED} plus an in-process per-queue
        \texttt{asyncio.Lock} (closes same-process interleaving); committed inside the lock.
  \item \textbf{Lease renewal:} worker heartbeats extend \texttt{lease\_expires\_at} on held jobs, so a
        long-running job is never prematurely reaped / double-executed.
  \item \textbf{Stale-lease reaper:} requeues (or dead-letters) jobs whose lease expired; closes the
        dangling execution row as \texttt{TIMED\_OUT}.
  \item \textbf{FAILED is real:} retriable failures sit in FAILED until backoff elapses, then the claim
        path promotes them to QUEUED (dashboard ``Failed'' count is meaningful).
  \item \textbf{Idempotency:} \texttt{UNIQUE(queue\_id, idempotency\_key)}; manual DLQ replay resets the
        attempt budget so replay actually retries.
  \item \textbf{Graceful shutdown:} SIGTERM/SIGINT $\to$ stop claiming, mark DRAINING, finish in-flight
        work (up to a timeout), deregister.
\end{itemize}

\section*{7. API Surface (selected)}
\begin{tabularx}{\textwidth}{l X}
\toprule
Group & Endpoints \\
\midrule
Auth & \texttt{POST /auth/register}, \texttt{POST /auth/login} \\
Projects & \texttt{POST/GET /organizations/\{org\}/projects} (returns API key once) \\
Queues & CRUD, \texttt{patch}, \texttt{pause/resume}, \texttt{stats} \\
Jobs & create (1 or batch), list (paginated/filtered), detail, \texttt{cancel}, \texttt{retry},
      scheduled-jobs, dead-letter-queue \\
Workers & \texttt{register}, \texttt{heartbeat}, \texttt{claim}, \texttt{start}, \texttt{result},
         \texttt{drain}, \texttt{deregister} \\
Dashboard & \texttt{health}, \texttt{queues}, \texttt{workers} (JWT); \texttt{WS /ws/projects/\{id\}?token=JWT} \\
\bottomrule
\end{tabularx}
All errors are structured: \texttt{\{error:\{code,message,details\}\}}; list endpoints are paginated.

\section*{8. Testing \& Quality}
\begin{itemize}
  \item Lifecycle, retry-then-DLQ, queue pause, dependency ordering, RBAC (VIEWER blocked / OWNER allowed).
  \item Real \texttt{FAILED} state + backoff promotion; scheduler promote/reap (requeue + TIMED\_OUT, and
        dead-letter when retries exhausted).
  \item Postgres-gated concurrency race: two workers, no duplicate/lost claims under real \texttt{SKIP LOCKED}.
  \item Structured error handling, JWT auth fail-fast in production, CORS hardened, WebSocket auth.
\end{itemize}

\section*{9. Running It}
\begin{verbatim}
docker compose up --build        # API :8000, Frontend :5173, Postgres :5432
# register -> create project (save the shown API key) -> start workers:
python -m app.worker.worker --base-url http://localhost:8000 \
  --api-key <PROJECT_API_KEY> --project-id 1 --queues default --concurrency 4
\end{verbatim}

\section*{10. Trade-offs / Future Work}
In-process rate-limit \& WebSocket fanout (fine for one replica; Redis/LISTEN-NOTIFY at multi-replica);
scheduler not leader-elected (idempotent, redundant today); no soft-delete; no event-driven push
(\texttt{LISTEN/NOTIFY}); queue sharding and AI failure summaries identified as next steps.
\end{document}
